# Repository-level configuration for lw_coder

# Required: Schema version for forward compatibility
schema_version = "1.0"

# Worktree file synchronization
[worktree.file_sync]
enabled = false
patterns = [
]

# Additional directories accessible to the SDK sandbox during `weft code`
[sandbox]
additional_dirs = [
    { path = "~/.cache/uv", if_missing = "create" },
]
